'use strict';

function CarFactory() {
  const Vehicle = ()=> ({

  drive: () =>
      ('Moving Forward'),

  stop: () =>
      ('Stopping')

})
};

function Car(name, wheels)


module.exports = {CarFactory, Car, Motorcycle};

